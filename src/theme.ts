import {createTheme} from "@mui/material";

export const theme = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#556cd6',
    },
    secondary: {
      main: '#19857b',
    },
    text: {
      primary: 'rgba(255, 255, 245, .86)',
    },
    background: {
      default: '#1e1e20'
    },
  },
});